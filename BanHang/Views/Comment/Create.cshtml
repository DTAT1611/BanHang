@model BanHang.Models.EF.Comment

<style>
    .addcmt::before {
        content: "bình luận"; /* Đây là mã Unicode của biểu tượng 📩 */
        font-size: 15px; /* Kích thước biểu tượng */

    }
    .addcmt {

        margin-top:70px;
        display:inline;
    }
</style>

<div id="create">
    @Html.AntiForgeryToken()
    @if (Request.IsAuthenticated)
    {
        {
            <div class="form-group col-6" style="display:inline-block" id="comment">

                <label for="exampleInputEmail1" style="display: inline; margin-right: 10px;">Bình luận</label>
                @Html.TextBoxFor(x => x.comms, new { @class = "form-control", id = "textboxcomment"})
               
            </div>
        }

<div class="col-3 text-left" style="display:inline-block;margin-top:50px">
    <a href="javascript:void(0);" id="btnclick" class="btnclick addcmt" onclick="AddCommnet();"></a>

</div>
    }

    else
    {
        <p>Đăng nhập để bình luận</p>
    }
</div>

